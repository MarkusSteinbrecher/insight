# /status

Run `python3 scripts/status.py --quick` and display the output. Then add any blockers or decisions needing sponsor input based on your knowledge of the current state.

If the sponsor asks for test results too, run `python3 scripts/status.py` (without --quick) instead.
